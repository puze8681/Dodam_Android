<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="kr.puze.dodam">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".SurveyActivity" />
        <activity android:name=".Chat.ChatThemeActivity" />
        <activity android:name=".Chat.ChatActivity" />
        <activity android:name=".Study.StudyInActivity" />
        <activity android:name=".Study.StudySelectActivity" />
        <activity android:name=".Study.StudyAnswerActivity"></activity>
    </application>

</manifest>
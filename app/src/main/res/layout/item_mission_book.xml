<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <data>
        <variable
            name="data_mission_book"
            type="kr.puze.dodam.Data.MissionBookData"/>
    </data>
    <android.support.constraint.ConstraintLayout
        android:layout_margin="16dp"
        android:layout_width="match_parent"
        android:layout_height="100dp">
        <ImageView
            tools:src="@drawable/ic_mission_book_lock"
            app:layout_constraintStart_toStartOf="parent"
            android:id="@+id/data_mission_book_image"
            android:src="@{data_mission_book.image}"
            android:layout_width="wrap_content"
            android:layout_height="match_parent" />
        <TextView
            android:id="@+id/data_mission_book_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@{data_mission_book.title, default=title"
            app:layout_constraintStart_toEndOf="@+id/data_mission_book_image"
            app:layout_constraintTop_toTopOf="parent"/>
        <TextView
            android:id="@+id/data_mission_book_status"
            app:layout_constraintStart_toEndOf="@id/data_mission_book_image"
            app:layout_constraintTop_toBottomOf="@id/data_mission_book_title"
            android:text="@{data_mission_book.status, default=status"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content" />
        <android.support.v4.widget.ContentLoadingProgressBar
            app:layout_constraintTop_toBottomOf="@id/data_mission_book_status"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toEndOf="@id/data_mission_book_image"
            app:layout_constraintBottom_toBottomOf="parent"
            style="?android:attr/progressBarStyleHorizontal"
            android:max="100"
            android:progress="50"
            android:layout_width="0dp"
            android:layout_height="0dp" />
    </android.support.constraint.ConstraintLayout>
</layout>